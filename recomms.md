# 📝 توصيات فريق العمل (Team Recommendations)

هذا الملف مخصص لمزامنة التوصيات، الإعدادات، وأفضل الممارسات (Best Practices) بين جميع أعضاء الفريق الذين يعملون على المشروع من أجهزة كمبيوتر مختلفة لضمان استقرار بيئة التطوير.
**يُرجى قراءة هذا الملف وتحديث بيئتك المحلية بناءً عليه.**

## 1. إعدادات النطاقات المحلية (Local Domains & Wildcards)

**مقدمة**
نظراً لأن منصة AQL تعتمد على العمارة متعددة المؤسسات (Multi-tenant) وكل مستأجر يحصل على نطاق فرعي خاص به `slug.domain.ext`:

- **نظام ويندوز وملف Hosts:** ملف الـ `hosts` في نظام Windows لا يدعم النطاقات الفرعية العشوائية (Wildcard `*.platform.local`).
- **التوصية الأفضل (Recommended):** لتجنب التعديل اليدوي على ملف `hosts` لكل مؤسسة جديدة، يُفضل تعديل النطاق المحلي في المتصفح وبيئة التطوير ليكون `.localhost` بدلاً من `.local` أو `.test`، لأن متصفحات Chrome و Edge توجه أي شيء ينتهي بـ `.localhost` إلى `127.0.0.1` تلقائياً.
- **تحديث ملف الـ `.env`**

APP_URL="http://platform.local"

## 2. إعدادات خادم Laragon

إذا كنت مصراً على استخدام `.local` أو `.test` وترغب في أن يدعم سيرفر أباتشي (Apache) في لاراجون النطاقات الفرعية:

- افتح ملف إعدادات الموقع الافتراضي `C:/laragon/etc/apache2/sites-enabled/platform.local.conf` (أو ما يعادله).
- أضف `ServerAlias *.platform.local` أسفل `ServerName`.
- أعد تشغيل (Restart) خادم أباتشي من لوحة تحكم لاراجون.

## 3. التعامل مع ملف `.env` وذاكرة التخزين المؤقت (Cache)

عند تحميل تحديثات جديدة من الـ Repository تتطلب تغييرات في ملف البيئة (مثل إضافة إعدادات SMTP للبريد الإلكتروني)، لا تنسَ تنفيذ الأمر التالي لمسح الكاش وتفعيل الإعدادات الجديدة:

php artisan config:clear

## 4. بناء واجهات الـ Frontend (React & Vite)

إذا قمت بجلب (Pull) تعديلات جديدة تتضمن تغييرات في مكونات React (`resources/js`) ولم تظهر لك التعديلات في المتصفح، تذكر دائماً تشغيل سيرفر التطوير أو إعادة بناء الملفات:

**للمشاهدة الحية أثناء التطوير**
npm run dev

**لبناء الملفات النهائية**
npm run build

## 5. قواعد تخطيط الواجهات (Layouts)

تم تطبيق قرار صارم بالفصل التام بين واجهات "المنصة المركزية" وواجهات "المستأجرين":

- **`PlatformLayout`**: يُستخدم **فقط** للصفحات الخاصة بمدير المنصة المركزية (`/admin/*`).
- **`TenantLayout`**: يُستخدم للصفحات الخاصة بالمؤسسات والمستأجرين (`/users`, `/audit`, إلخ). يحتوي على قائمة جانبية مخصصة لا تتضمن أي وصول لمعلومات المنصة.
  _الرجاء الالتزام بهذا الفصل عند برمجة أي موديول جديد._
