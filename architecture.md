# Architecture

## المحتويات

## Table of Contents

01. [نظرة عامة (Overview)](#1-نظرة-عامة-overview)
02. [الأهداف المعمارية (Architectural Goals)](#2-الأهداف-المعمارية-architectural-goals)
03. [المبادئ المعمارية (Architectural Principles)](#3-المبادئ-المعمارية-architectural-principles)
04. [المعمارية عالية المستوى (High-Level Architecture)](#4-المعمارية-عالية-المستوى-high-level-architecture)
05. [طبقات المنصة (Platform Layers)](#5-طبقات-المنصة-platform-layers)
06. [حدود التطبيقات (Application Boundaries)](#6-حدود-التطبيقات-application-boundaries)
07. [معمارية الوحدات (Module Architecture)](#7-معمارية-الوحدات-module-architecture)
08. [معمارية الاتصال (Communication Architecture)](#8-معمارية-الاتصال-communication-architecture)
09. [معمارية الأحداث (Event Architecture)](#9-معمارية-الأحداث-event-architecture)
10. [معمارية الهوية والصلاحيات (Identity & Authorization Architecture)](#10-معمارية-الهوية-والصلاحيات-identity--authorization-architecture)
11. [معمارية الإعدادات (Settings Architecture)](#11-معمارية-الإعدادات-settings-architecture)
12. [التوطين ودعم اللغات (Localization)](#12-التوطين-ودعم-اللغات-localization)
13. [معمارية واجهة المستخدم (UI Architecture)](#13-معمارية-واجهة-المستخدم-ui-architecture)
14. [الملفات والتخزين (Files & Storage)](#14-الملفات-والتخزين-files--storage)
15. [التقارير والطباعة (Reporting & Printing)](#15-التقارير-والطباعة-reporting--printing)
16. [التدقيق والنشاط (Audit & Activity)](#16-التدقيق-والنشاط-audit--activity)
17. [الأمان (Security)](#17-الأمان-security)
18. [معمارية واجهات API (API Architecture)](#18-معمارية-واجهات-api-api-architecture)
19. [معمارية البيانات (Data Architecture)](#19-معمارية-البيانات-data-architecture)
20. [المعالجة في الخلفية (Background Processing)](#20-المعالجة-في-الخلفية-background-processing)
21. [قابلية المراقبة والرصد (Observability)](#21-قابلية-المراقبة-والرصد-observability)
22. [معمارية النشر (Deployment Architecture)](#22-معمارية-النشر-deployment-architecture)
23. [معمارية التكامل (Integration Architecture)](#23-معمارية-التكامل-integration-architecture)
24. [الإصدارات والتوافق (Versioning & Compatibility)](#24-الإصدارات-والتوافق-versioning--compatibility)
25. [الحالة الحالية للمعمارية (Current Architecture Status)](#25-الحالة-الحالية-للمعمارية-current-architecture-status)
26. [مراجع القرارات المعمارية (Architecture Decision References)](#26-مراجع-القرارات-المعمارية-architecture-decision-references)

## 1. نظرة عامة (Overview)

### 1.1 الهدف

هذا المشروع عبارة عن **منصة بنية تحتية تكاملية وقابلة لإعادة الاستخدام**، توفر المكونات والخدمات الأساسية المشتركة التي تحتاج إليها مجموعة من تطبيقات الأعمال المختلفة.

تمثل المنصة الأساس المشترك الذي تُبنى فوقه تطبيقات مثل:

- إدارة المخازن
- المبيعات
- المالية
- المشتريات
- إدارة الفرق
- إدارة المصروفات
- وأي تطبيقات مستقبلية أخرى

الهدف الرئيسي هو **بناء البنية المشتركة مرة واحدة**، ثم تمكين التطبيقات المختلفة من استخدامها دون إعادة بناء نفس الوظائف والخدمات بشكل مستقل داخل كل تطبيق.

---

### 1.2 مفهوم المنصة

يتم التعامل مع المنصة باعتبارها **طبقة تشغيل للتطبيقات (Application Operating Layer)**، وليست تطبيقًا تجاريًا واحدًا.

توفر المنصة مجموعة من القدرات والخدمات المشتركة، من أهمها:

- الهوية والمستخدمون (Identity)
- المصادقة (Authentication)
- الصلاحيات والتحكم في الوصول (Authorization)
- المؤسسات ونطاقات العمل (Organizations / Tenancy)
- الإعدادات والتهيئة (Settings / Configuration)
- اللغات والتوطين (Localization)
- التنبيهات (Notifications)
- الملفات والمرفقات (Files / Attachments)
- سجل العمليات والنشاط (Audit / Activity)
- التقارير والطباعة (Reports / Printing)
- مكونات واجهة المستخدم والنماذج (UI / Forms)
- إدارة التطبيقات والموديولات (Module Management)
- إدارة الإصدارات (Versioning)
- الأحداث والتكاملات (Events / Integrations)

وتُبنى تطبيقات الأعمال المستقبلية باعتبارها **تطبيقات أو موديولات تعمل فوق هذه المنصة**.

---

### 1.3 المبدأ الأساسي

> **نبني القدرات المشتركة مرة واحدة، ونوفرها من خلال عقود واضحة، ثم نعيد استخدامها عبر التطبيقات.**

يجب على التطبيق استخدام خدمات المنصة عندما تكون الوظيفة المطلوبة متوفرة بالفعل فيها، بدلًا من إنشاء تنفيذ مستقل لها.

أمثلة:

- التطبيقات تستخدم نظام الهوية المركزي للمنصة.
- التطبيقات تستخدم نظام الصلاحيات المركزي.
- التطبيقات تستخدم خدمة التنبيهات.
- التطبيقات تستخدم خدمة الملفات والمرفقات.
- التطبيقات تستخدم نظام تسجيل العمليات (Audit).

---

### 1.4 الحدود المعمارية (Architectural Boundary)

ينقسم النظام بصورة مفاهيمية إلى ثلاث طبقات رئيسية:

```text
┌──────────────────────────────────────┐
│              التطبيقات              │
│                                      │
│ المخازن │ المبيعات │ المالية │ ...  │
└──────────────────┬───────────────────┘
                   │
                   ▼
┌──────────────────────────────────────┐
│               المنصة                │
│                                      │
│ النواة الأساسية + الخدمات المشتركة │
└──────────────────┬───────────────────┘
                   │
                   ▼
┌──────────────────────────────────────┐
│          البنية التحتية             │
│                                      │
│ قاعدة البيانات │ التخزين │ Cache    │
│ Queue │ Runtime │ الشبكات │ النشر   │
└──────────────────────────────────────┘
```

**التطبيقات تعتمد على المنصة.**

**المنصة تعتمد على البنية التحتية.**

ولا يجب أن تحتوي طبقة البنية التحتية على منطق أعمال خاص بتطبيق معين.

---

### 1.5 نطاق المرحلة الحالية

النطاق الأولي للمشروع **محدود عمدًا**.

الهدف الأول هو إنشاء أساس مستقر وقابل لإعادة الاستخدام للمنصة، ثم التحقق من صلاحيته من خلال بناء تطبيق صغير يعمل فوقها.

سيتم تطوير المنصة بصورة تدريجية، بحيث يتم إضافة القدرات المشتركة الجديدة عندما تكشف التطبيقات الفعلية عن الحاجة إليها.

---

### 1.6 ما لا تهدف إليه المرحلة الأولى

المنصة في مرحلتها الأولى ليست مخصصة لـ:

- تنفيذ جميع الوظائف التجارية الممكنة.
- بناء جميع التطبيقات المستقبلية مسبقًا.
- توقع جميع التكاملات المستقبلية.
- إضافة بنية تحتية معقدة قبل الحاجة إليها.
- تحويل المنصة إلى تطبيق أعمال ضخم يحتوي على جميع المجالات.

يجب أن تفضل القرارات المعمارية:

**البساطة، وإعادة الاستخدام، ووضوح الحدود، والتطور التدريجي.**

## 2. الأهداف المعمارية (Architectural Goals)

تهدف المعمارية إلى إنشاء منصة مستقرة وقابلة للتوسع، تستطيع استضافة تطبيقات متعددة مع الحفاظ على البساطة ووضوح المسؤوليات.

### 2.1 إعادة الاستخدام (Reusability)

بناء المكونات والخدمات المشتركة مرة واحدة، وإتاحتها لجميع التطبيقات التي تحتاج إليها.

يجب تجنب إعادة تنفيذ نفس الوظيفة داخل أكثر من تطبيق.

---

### 2.2 الفصل بين المنصة والتطبيقات (Separation of Concerns)

يجب الفصل بوضوح بين:

- خدمات وقدرات المنصة العامة.
- منطق الأعمال الخاص بكل تطبيق.
- البنية التحتية التقنية.

لا ينبغي أن يعتمد تطبيق على تفاصيل داخلية لتطبيق آخر.

---

### 2.3 قابلية إضافة تطبيقات جديدة (Extensibility)

يجب أن يكون بالإمكان إضافة تطبيق جديد إلى المنصة دون الحاجة إلى تعديل جوهري في النواة الأساسية.

التطبيق الجديد يجب أن يستطيع الاستفادة من الخدمات المشتركة الموجودة مسبقًا.

---

### 2.4 وضوح العقود (Clear Contracts)

يجب أن تتعامل التطبيقات مع خدمات المنصة من خلال **واجهات وعقود واضحة**، بدل الاعتماد على التفاصيل الداخلية لتنفيذ تلك الخدمات.

أي تغيير داخلي في المنصة لا ينبغي أن يؤدي إلى كسر التطبيقات طالما لم يتغير العقد المتفق عليه.

---

### 2.5 الأمان كافتراضي (Security by Default)

يجب أن تكون آليات الأمان جزءًا أساسيًا من تصميم المنصة، وليس وظيفة يضيفها كل تطبيق على حدة.

ويشمل ذلك على الأقل:

- المصادقة.
- الصلاحيات.
- التحكم في الوصول.
- حماية البيانات.
- تسجيل العمليات الحساسة.

---

### 2.6 قابلية التتبع (Auditability)

يجب أن تكون العمليات المهمة قابلة للتتبع ومعرفة:

- من قام بالعملية.
- ماذا حدث.
- على أي كيان.
- متى حدث.
- وما التغيير الذي تم.

ويجب توفير هذه القدرة كخدمة مشتركة للمنصة.

---

### 2.7 التطور التدريجي (Incremental Evolution)

لا يجب بناء جميع مكونات المنصة المستقبلية مسبقًا.

يتم تطوير المنصة تدريجيًا بناءً على الاحتياجات الفعلية التي تظهر أثناء بناء التطبيقات واستخدامها.

كل مرحلة يجب أن تضيف قيمة حقيقية وقابلة للاختبار.

---

### 2.8 البساطة (Simplicity)

يجب أن يكون الحل المعماري هو **أبسط حل قادر على تلبية المتطلبات الحالية والمستقبلية المعقولة**.

لا تتم إضافة تقنية أو خدمة أو طبقة جديدة لمجرد أنها متاحة أو شائعة.

---

### 2.9 قابلية الاختبار (Testability)

يجب تصميم مكونات المنصة والتطبيقات بحيث يمكن اختبارها بشكل مستقل قدر الإمكان.

ويشمل ذلك:

- اختبار الوحدات.
- اختبار الخدمات.
- اختبار العقود.
- اختبار التكامل.
- اختبار واجهة المستخدم عند الحاجة.

---

### 2.10 قابلية الصيانة (Maintainability)

يجب أن تكون مسؤوليات المكونات واضحة، وأن يكون من السهل:

- فهم الكود.
- اكتشاف الأخطاء.
- تعديل المكونات.
- استبدال مكون عند الحاجة.
- تطوير تطبيقات جديدة دون معرفة التفاصيل الداخلية لكل مكونات المنصة.

---

### 2.11 الاستقلال النسبي للتطبيقات (Application Independence)

يجب أن تكون التطبيقات مستقلة في منطق أعمالها وبياناتها قدر الإمكان، مع اعتمادها على الخدمات المشتركة التي توفرها المنصة.

الهدف هو تقليل الترابط المباشر بين التطبيقات، بحيث لا يؤدي تغيير تطبيق إلى كسر التطبيقات الأخرى.

---

### 2.12 قابلية التكامل (Integrability)

يجب أن تكون المنصة مهيأة للتكامل مع الأنظمة والخدمات الخارجية من خلال آليات واضحة وموحدة، مثل:

- APIs
- Events
- Webhooks
- Import / Export
- خدمات خارجية

مع تجنب ربط منطق الأعمال مباشرة بتفاصيل مزود خارجي متى كان ذلك ممكنًا.

## 3. المبادئ المعمارية (Architectural Principles)

هذه المبادئ ليست إرشادات اختيارية، وإنما تمثل **القواعد الأساسية التي يجب أن توجه القرارات المعمارية والتنفيذية في المشروع**.

أي استثناء عن هذه المبادئ يجب أن يكون مبررًا بشكل واضح، وأن يتم تسجيله كقرار معماري في `ARCHITECTURE_DECISIONS.md`.

---

### 3.1 المنصة أولًا — Platform First

> أي قدرة أو خدمة مشتركة بين تطبيقين أو أكثر يجب أن تُدرس أولًا باعتبارها مسؤولية للمنصة، وليس مسؤولية كل تطبيق على حدة.

لا يعني ذلك وضع جميع الوظائف داخل النواة الأساسية، وإنما تحديد المكان الصحيح لكل قدرة مشتركة وفق حدود واضحة.

---

### 3.2 إعادة الاستخدام قبل التكرار — Reuse Before Duplication

> قبل إنشاء مكون أو خدمة جديدة داخل تطبيق، يجب التحقق مما إذا كانت المنصة توفر بالفعل قدرة تؤدي نفس الغرض أو يمكن توسيعها بشكل مناسب.

يجب تجنب إعادة تنفيذ نفس الوظيفة داخل أكثر من تطبيق.

ولا يعني ذلك تحويل كل شيء إلى مكونات عامة (Generic) بشكل مبالغ فيه؛ فالوضوح والبساطة أهم من التعميم غير الضروري.

---

### 3.3 حدود واضحة — Clear Boundaries

> لكل مكون أو Module أو Application مسؤولية واضحة، ولا يجوز أن يعتمد على التفاصيل الداخلية لمكونات أخرى.

يجب أن يتم التواصل بين المكونات من خلال الواجهات والعقود المحددة لها، بدل الوصول المباشر إلى تفاصيل التنفيذ الداخلية.

---

### 3.4 العقود قبل التفاصيل — Contracts Before Implementation

> يجب تحديد كيفية تعامل المكونات مع بعضها من خلال عقود واضحة قبل السماح للتطبيقات بالاعتماد على تفاصيل التنفيذ الداخلية.

يمكن تغيير طريقة تنفيذ الخدمة داخليًا طالما تم الحفاظ على العقد المتفق عليه مع المستهلكين.

---

### 3.5 الأمان افتراضيًا — Security by Default

> يجب أن تكون آليات الأمان جزءًا أساسيًا من تصميم المنصة، وأن تكون الحالة الافتراضية للمكونات الجديدة آمنة.

لا ينبغي الاعتماد على قيام كل مطور أو تطبيق بإضافة الحماية يدويًا بعد بناء الوظيفة.

---

### 3.6 أقل صلاحية لازمة — Least Privilege

> يجب أن يحصل كل مستخدم أو Module أو Service على الحد الأدنى من الصلاحيات اللازمة لأداء مهمته.

يجب تصميم نظام الصلاحيات بحيث يسمح بالتحكم الدقيق في العمليات، دون فرض تعقيد غير ضروري في المراحل الأولى.

---

### 3.7 لا منطق أعمال في البنية التحتية — No Business Logic in Infrastructure

> البنية التحتية توفر الإمكانيات التقنية، لكنها لا تعرف قواعد العمل الخاصة بتطبيق معين.

أمثلة على مسؤوليات البنية التحتية:

- تخزين البيانات.
- تشغيل المهام في الخلفية.
- تخزين الملفات.
- توفير الاتصال والخدمات التقنية.

أما قواعد الأعمال الخاصة بالمخازن أو المبيعات أو المالية، فتنتمي إلى نطاق التطبيق أو المجال الذي يملكها.

---

### 3.8 الأحداث قبل الترابط المباشر — Events Over Coupling

> عندما لا تكون العلاقة بين مكونين بحاجة إلى استجابة مباشرة وفورية، يُفضّل استخدام الأحداث (Events) لتقليل الترابط المباشر بينهما.

يجب عدم استخدام الأحداث لمجرد وجود Event Bus؛ بل عندما تحقق فائدة حقيقية في تقليل الترابط أو فصل المسؤوليات.

---

### 3.9 الإعدادات بدل القيم الثابتة — Configuration Over Hard-Coding

> القيم التي يمكن أن تختلف حسب البيئة أو المؤسسة أو التطبيق أو المستخدم يجب ألا تُدفن داخل الكود دون سبب.

ويجب في الوقت نفسه تجنب تحويل كل قيمة ثابتة إلى إعداد قابل للتغيير دون حاجة حقيقية.

---

### 3.10 التوافق قبل التغيير — Compatibility Before Change

> لا يجوز إجراء تغيير في Contract أو API أو خدمة مشتركة يؤدي إلى كسر التطبيقات القائمة دون استراتيجية واضحة للإصدار والانتقال.

عند الحاجة إلى تغيير غير متوافق، يجب استخدام آلية مناسبة مثل:

- Versioning
- Migration
- Deprecation
- Backward Compatibility

---

### 3.11 الاختبار جزء من البناء — Test as Part of Development

> لا يُعتبر المكون مكتملًا بمجرد كتابة الكود؛ بل يجب أن تكون له اختبارات مناسبة لطبيعته وأهميته.

ويشمل ذلك، حسب الحاجة:

- Unit Tests
- Integration Tests
- Contract Tests
- End-to-End Tests

لا يشترط تحقيق تغطية اختبارية كاملة لكل أجزاء النظام، وإنما يجب تحقيق مستوى اختبار مناسب للمخاطر والمسؤولية.

---

### 3.12 لا نبني ما لا نحتاجه — Build What We Need

> لا تتم إضافة مكون أو طبقة أو تقنية إلى المنصة لمجرد احتمال استخدامها مستقبلًا.

يجب أن يكون لكل مكون مبرر حقيقي مرتبط بمتطلب حالي أو بقرار معماري ضروري لحماية قابلية التطور.

الهدف هو تجنب الـ Overengineering مع الحفاظ على القدرة على التطور مستقبلًا.

---

### 3.13 القرار المعماري المبرر — Decisions Must Be Explicit

> القرارات التي تؤثر على بنية النظام أو تقنياته أو حدوده يجب أن تكون معلنة ومبررة ومسجلة.

القرارات المهمة يجب تسجيلها في:

`ARCHITECTURE_DECISIONS.md`

ويجب أن يتضمن القرار، عند الحاجة:

- السياق.
- المشكلة.
- البدائل.
- القرار.
- أسباب الاختيار.
- النتائج والتأثيرات.

---

### 3.14 الأدوات تتبع المعمارية — Tools Follow Architecture

> أدوات التطوير والمساعدات البرمجية والذكاء الاصطناعي أدوات لتنفيذ وتحسين النظام، وليست مصدرًا تلقائيًا للقرارات المعمارية.

يجب أن تعمل أدوات مثل Antigravity داخل الحدود والقرارات المعمارية المعتمدة.

يمكن للأداة:

- اقتراح حلول.
- كتابة الكود.
- تحسين الكود.
- إنشاء الاختبارات.
- اكتشاف المشاكل.
- اقتراح بدائل.

لكن القرارات المعمارية الجوهرية يجب أن تكون واضحة ومراجعة ومسجلة.

## 4. المعمارية عالية المستوى (High-Level Architecture)

### 4.1 التصور العام

يتكون النظام من ثلاث مناطق معمارية رئيسية:

```text
┌─────────────────────────────────────────────────────┐
│                    APPLICATIONS                     │
│                                                     │
│   Inventory   │   Sales   │   Finance   │   ...    │
└───────────────────────┬─────────────────────────────┘
                        │
                        │ Platform Contracts
                        ▼
┌──────────────────────────────────────────────────────┐
│                     PLATFORM                         │
│                                                      │
│  Core Kernel             │       Shared Services     │
│  ─────────────           │       ────────────────    │
│  Identity                │       Notifications       │
│  Authentication          │       Files               │
│  Authorization           │       Reports             │
│  Organizations           │       Printing            │
│  Settings                │       Search              │
│  Localization            │       Events              │
│  Audit                   │       Jobs                │
│  Modules                 │       Integrations        │
│  Versioning              │       ...                 │
└───────────────────────┬──────────────────────────────┘
                        │
                        │ Infrastructure Contracts
                        ▼
┌──────────────────────────────────────────────────────┐
│                 INFRASTRUCTURE                       │
│                                                      │
│ Database │ Cache │ Storage │ Queue │ Runtime         │
│ Networking │ Logging │ Deployment │ External Systems │
└──────────────────────────────────────────────────────┘
```

---

### 4.2 طبقة التطبيقات — Applications Layer

تمثل هذه الطبقة تطبيقات الأعمال التي تحقق احتياجات محددة.

أمثلة:

- إدارة المخازن (Inventory)
- المبيعات (Sales)
- المالية (Finance)
- المشتريات (Purchasing)
- إدارة الفرق (Teams)
- إدارة المصروفات (Expenses)

كل Application يمتلك منطق الأعمال الخاص به، وبياناته، وواجهاته، ومكوناته الخاصة بالمجال الذي يخدمه.

#### 4.2.1 مسؤوليات Application

- تنفيذ قواعد الأعمال الخاصة بالمجال.
- إدارة حالات وعمليات المجال.
- تقديم واجهات المستخدم الخاصة بالمجال.
- استخدام خدمات الـ Platform.
- نشر الأحداث الخاصة بالمجال.
- الاستجابة للأحداث التي يحتاج إليها.
- تعريف الصلاحيات والإعدادات الخاصة به.

#### 4.2.2 لا مسؤوليات Application

لا يجب على التطبيق إعادة بناء خدمات توفرها المنصة بالفعل، مثل:

- Authentication
- User Management
- Authorization
- Notifications
- File Management
- Audit
- Localization

---

### 4.3 طبقة المنصة — Platform Layer

تمثل المنصة الطبقة المشتركة التي تعتمد عليها التطبيقات.

تنقسم المنصة إلى قسمين رئيسيين:

#### 4.3.1 النواة الأساسية — Core Kernel

تحتوي على القدرات الأساسية التي تشكل هوية المنصة.

وتشمل مبدئيًا:

- Identity
- Users
- Organizations / Tenancy
- Authentication
- Authorization
- Settings
- Localization
- Audit
- Module Registry
- Versioning

يجب أن تظل النواة صغيرة قدر الإمكان، ولا يتم إدخال وظيفة إليها إلا عندما تكون مسؤولية مركزية فعلًا للمنصة.

---

#### 4.3.2 الخدمات المشتركة — Shared Services

توفر خدمات قابلة لإعادة الاستخدام من قبل التطبيقات.

وتشمل مبدئيًا:

- Notifications
- Files / Attachments
- Documents
- Reports
- Printing
- Templates
- Search
- Activity
- Import / Export
- Background Jobs
- Webhooks
- Events / Event Bus
- Integrations

يمكن إضافة خدمات جديدة مع تطور احتياجات التطبيقات.

---

### 4.4 طبقة البنية التحتية — Infrastructure Layer

تمثل الإمكانيات التقنية التي تحتاج إليها المنصة والتطبيقات لتشغيل النظام.

أمثلة:

- Database
- Cache
- File Storage
- Queue
- Runtime
- Networking
- Logging
- Monitoring
- Deployment
- External Infrastructure

هذه الطبقة لا تحتوي على قواعد الأعمال الخاصة بأي Application.

ويجب أن تتعامل الطبقات الأعلى معها من خلال abstractions أو contracts مناسبة عندما يكون ذلك مفيدًا.

---

## 4.5 اتجاه الاعتماد — Dependency Direction

الاتجاه الأساسي للاعتماد هو:

```text
Applications
     │
     ▼
Platform
     │
     ▼
Infrastructure
```

لا ينبغي أن يحدث اعتماد عكسي يؤدي إلى معرفة Infrastructure بتفاصيل Business Logic الخاصة بالتطبيقات.

كما يجب تجنب الاعتماد المباشر بين Applications قدر الإمكان.

عند الحاجة إلى التواصل بين التطبيقات، يتم استخدام آلية مناسبة مثل:

- Platform Service
- API Contract
- Domain Event
- Integration Event

بحسب طبيعة العلاقة.

---

## 4.6 العلاقة بين التطبيقات والمنصة

التطبيقات تعتبر **مستهلكًا لخدمات المنصة**.

```text
┌───────────────┐
│  Application  │
└───────┬───────┘
        │
        │ Uses
        ▼
┌───────────────┐
│   Platform    │
│   Services    │
└───────────────┘
```

التطبيق لا يحتاج إلى معرفة كيفية تنفيذ خدمة المنصة داخليًا.

مثال:

```text
Application
    │
    ├── Authentication
    ├── Permission Check
    ├── Send Notification
    ├── Store File
    ├── Write Audit
    └── Publish Event
             │
             ▼
          Platform
```

---

## 4.7 العلاقة بين التطبيقات

الأصل هو **تقليل الاعتماد المباشر بين التطبيقات**.

مثال غير مرغوب:

```text
Sales ───────────────► Inventory
       Direct Dependency
```

والبديل المفضل عند ملاءمته:

```text
Sales
  │
  │ OrderConfirmed
  ▼
Event Bus
  │
  ▼
Inventory
```

لكن استخدام Events ليس قاعدة مطلقة؛ فإذا كانت هناك حاجة حقيقية لاستجابة مباشرة، يمكن استخدام Contract أو API مناسب.

---

## 4.8 مبدأ ملكية البيانات — Data Ownership

كل Application مسؤول عن بيانات المجال التي يملكها.

مثال:

```text
Inventory
   └── Inventory Data

Sales
   └── Sales Data

Finance
   └── Financial Data
```

لا ينبغي لتطبيق أن يتلاعب مباشرة ببيانات مملوكة لتطبيق آخر.

إذا احتاج تطبيق إلى بيانات من تطبيق آخر، يجب الوصول إليها من خلال آلية اتصال معتمدة.

تفاصيل استراتيجية ملكية البيانات ومشاركة البيانات سيتم تحديدها لاحقًا بعد اختيار الـ Tech Stack ونموذج الـ Deployment.

---

## 4.9 مبدأ عدم الإفراط في التقسيم

لا يشترط أن تكون كل طبقة أو خدمة أو Application عملية مستقلة (Independent Process أو Microservice).

يمكن أن تعمل عدة مكونات داخل نفس التطبيق أو نفس عملية التشغيل طالما أن الحدود المنطقية بينها واضحة.

الهدف الأساسي هو:

> **وضوح الحدود وتقليل الترابط، وليس زيادة عدد الخدمات والعمليات.**

---

## 4.10 التطور المتوقع للمعمارية

يجب أن تسمح المعمارية بالانتقال مستقبلًا من:

```text
Modular Application
```

إلى:

```text
Multiple Applications
```

وإلى فصل بعض الخدمات أو التطبيقات إلى عمليات مستقلة عند وجود سبب حقيقي لذلك.

لا يتم إنشاء Microservices أو بنية موزعة مسبقًا إلا عندما تبررها المتطلبات الفعلية.

---

## 4.11 الحالة الحالية

في الإصدار `v0.1` يتم اعتماد هذا النموذج باعتباره **التصور المعماري المبدئي**.

التفاصيل المتعلقة بالتقنيات والتنفيذ سيتم تحديدها لاحقًا من خلال قرارات معمارية مستقلة.

الهدف الحالي هو تثبيت:

1. حدود Applications.
2. حدود Platform.
3. حدود Infrastructure.
4. اتجاه الاعتماد.
5. ملكية البيانات.
6. آليات التواصل العامة.
7. إمكانية التطور مستقبلًا دون فرض تعقيد مبكر.

## 5. طبقات المنصة (Platform Layers)

تمثل طبقات المنصة مجموعة القدرات المشتركة التي تعتمد عليها التطبيقات.

تنقسم المنصة إلى ثلاث مناطق رئيسية:

```text
┌──────────────────────────────────────────────┐
│              Application Shell               │
│     الواجهة المشتركة وإطار تشغيل التطبيقات   │
└──────────────────────┬───────────────────────┘
                       │
┌──────────────────────▼───────────────────────┐
│                 Core Kernel                  │
│       القدرات الأساسية لهوية المنصة          │
└──────────────────────┬───────────────────────┘
                       │
┌──────────────────────▼───────────────────────┐
│              Shared Services                │
│       الخدمات المشتركة القابلة لإعادة الاستخدام │
└──────────────────────────────────────────────┘
```

---

### 5.1 غلاف التطبيق — Application Shell

يمثل الـ Application Shell الإطار المشترك الذي تعمل داخله جميع التطبيقات.

الغرض منه هو توفير تجربة تشغيل وواجهة موحدة، دون إدخال منطق الأعمال الخاص بأي تطبيق.

#### 5.1.1 مسؤوليات Application Shell

- التخطيط العام للواجهة (Layout).
- التنقل الرئيسي (Navigation).
- قائمة المستخدم.
- اختيار التطبيق أو الموديول (Application / Module Switcher).
- منطقة التنبيهات.
- إعدادات المستخدم.
- اختيار اللغة.
- دعم RTL / LTR.
- الحالات العامة للواجهة.
- إدارة الجلسة على مستوى الواجهة.
- المكونات الأساسية المشتركة.

#### 5.1.2 لا يحتوي Application Shell على:

- قواعد أعمال خاصة بتطبيق معين.
- شاشات خاصة بالمخازن أو المبيعات أو المالية.
- منطق خاص بمجال تجاري محدد.

مثال:

```text id="2x7zwr"
Application Shell
│
├── Header
├── Navigation
├── User Menu
├── Module Switcher
├── Notifications
└── Application Area
         │
         ├── Inventory
         ├── Sales
         └── Finance
```

---

### 5.2 النواة الأساسية — Core Kernel

تمثل النواة الحد الأدنى من القدرات التي تشكل هوية المنصة نفسها.

يجب أن تبقى النواة **صغيرة ومستقرة**، ولا تتم إضافة أي وظيفة إليها إلا إذا كانت مسؤوليتها مركزية للمنصة وليست مجرد خدمة مشتركة عادية.

### مكونات Core Kernel

#### 5.2.1 الهوية والمستخدمون — Identity & Users

مسؤول عن:

- تعريف المستخدمين.
- بيانات الهوية الأساسية.
- حالة المستخدم.
- ارتباط المستخدم بالمؤسسة أو نطاق العمل.
- المعلومات الأساسية اللازمة للتعرف على المستخدم.

---

#### 5.2.2 المؤسسات ونطاقات العمل — Organizations / Tenancy

يوفر إطارًا لتمثيل المؤسسة أو نطاق العمل الذي تعمل داخله التطبيقات.

يسمح للمنصة بربط:

```text id="1z2l0d"
Organization
    │
    ├── Users
    ├── Roles
    ├── Settings
    └── Applications
```

يجب ألا نفترض من البداية تفاصيل نموذج Multi-Tenancy النهائي قبل اتخاذ القرار المعماري الخاص به.

---

#### 5.2.3 المصادقة — Authentication

مسؤول عن إثبات هوية المستخدم والتحكم في جلسات الدخول.

يشمل مبدئيًا:

- Login
- Logout
- Session
- Password Management
- Password Reset
- Email Verification

ويمكن إضافة آليات أخرى مستقبلًا مثل:

- MFA
- SSO
- OAuth / External Identity Providers

وفقًا لاحتياجات المشروع.

---

#### 5.2.4 الصلاحيات والتحكم في الوصول — Authorization

مسؤول عن تحديد ما يمكن للمستخدم أو أي كيان مصرح له القيام به.

يعتمد النظام مبدئيًا على مفهوم:

```text id="w4v0yq"
User
  ↓
Role
  ↓
Permissions
```

ويجب أن تكون الصلاحيات قابلة للتعبير عن عمليات واضحة، مثل:

```text id="a7z2z1"
inventory.item.view
inventory.item.create
inventory.item.edit
inventory.item.delete
inventory.item.approve
```

يمكن توسيع نموذج الصلاحيات لاحقًا ليشمل Scopes أو قيودًا على مستوى المؤسسة أو الفرع أو الموارد.

---

#### 5.2.5 الإعدادات الأساسية — Settings

توفر المنصة نظامًا موحدًا لإدارة الإعدادات.

يجب أن يدعم النظام منطقيًا مستويات مختلفة مثل:

```text id="0f4n0v"
Platform Settings
       │
Organization Settings
       │
Application Settings
       │
User Settings
```

وتفاصيل آلية التخزين والأولوية بين المستويات سيتم تحديدها لاحقًا.

---

#### 5.2.6 التوطين واللغة — Localization

توفر المنصة خدمات موحدة لـ:

- اللغات.
- الترجمة.
- RTL / LTR.
- تنسيق التاريخ.
- تنسيق الأرقام.
- العملات.
- المناطق الزمنية.

يجب ألا يقوم كل Application ببناء نظام ترجمة أو Localization مستقل.

---

#### 5.2.7 سجل العمليات — Audit

يوفر النظام المركزي لتسجيل العمليات المهمة.

يجب أن يسمح، حسب طبيعة العملية، بمعرفة:

- من قام بالعملية.
- ماذا حدث.
- الكيان المتأثر.
- وقت العملية.
- القيم السابقة.
- القيم الجديدة.

يجب ألا يعتمد التطبيق على إنشاء Audit System خاص به إذا كانت العملية يمكن تسجيلها من خلال خدمة المنصة.

---

#### 5.2.8 سجل التطبيقات والموديولات — Module Registry

مسؤول عن تعريف التطبيقات والموديولات المسجلة داخل المنصة.

يحتوي منطقيًا على معلومات مثل:

- Module ID
- Name
- Version
- Status
- Routes
- Menus
- Permissions
- Settings
- Dependencies

مثال:

```text id="7j1v0b"
Inventory
├── Version
├── Routes
├── Menus
├── Permissions
└── Settings
```

---

#### 5.2.9 إدارة الإصدارات — Versioning

توفر المنصة إطارًا لإدارة إصدارات:

- Platform
- Applications
- Modules
- APIs / Contracts

يجب أن تدعم آلية الإصدار التطور التدريجي والحفاظ على التوافق عند الحاجة.

---

### 5.3 الخدمات المشتركة — Shared Services

الخدمات المشتركة هي قدرات يمكن أن تستخدمها تطبيقات متعددة، لكنها لا تشكل بالضرورة جزءًا من هوية الـ Core Kernel.

يمكن تطوير هذه الخدمات وتوسيعها بصورة أكثر استقلالًا من مكونات النواة.

---

#### 5.3.1 التنبيهات — Notifications

خدمة مركزية لإرسال التنبيهات.

تدعم منطقيًا:

```text id="5y6fgi"
Event
  ↓
Notification
  ↓
Channel
 ├── In-App
 ├── Email
 ├── SMS
 └── Push
```

ويتم تحديد القنوات الفعلية حسب احتياجات المشروع.

---

#### 5.3.2 الملفات والمرفقات — Files / Attachments

خدمة موحدة لإدارة الملفات المرتبطة بكيانات النظام.

أمثلة:

```text id="j3w9r7"
Invoice
 └── Attachments

Employee
 └── Documents

Product
 └── Images
```

تتعامل الخدمة مع:

- رفع الملفات.
- تخزينها.
- ربطها بالكيانات.
- الوصول إليها.
- إدارة بياناتها.
- الإصدارات عند الحاجة.

---

#### 5.3.3 المستندات — Documents

خدمة لإدارة المستندات التي تتجاوز مجرد الملفات المرفقة، عندما يكون للمستند دورة حياة أو بيانات وصفية أو حالة أو إصدار.

يمكن أن تشمل مستقبلًا:

- Document Metadata
- Document Versions
- Document Status
- Document Templates

ويجب الفصل بين مفهوم **File** ومفهوم **Business Document** عند الحاجة.

---

#### 5.3.4 التقارير — Reports

خدمة مشتركة لتوليد التقارير من بيانات التطبيقات.

تهدف إلى فصل:

```text id="8f4bpl"
Data
 ↓
Report Definition
 ↓
Template
 ↓
Renderer
 ↓
Output
```

عن منطق الأعمال الخاص بالتطبيق.

---

#### 5.3.5 الطباعة والتصدير — Printing / Export

توفر المنصة آليات مشتركة لإخراج البيانات والمستندات بصيغ مختلفة، مثل:

- Print
- PDF
- Excel
- CSV

يجب ألا يقوم كل Application ببناء آلية مستقلة لإنتاج التقارير والمطبوعات المشتركة.

---

#### 5.3.6 القوالب — Templates

خدمة لإدارة القوالب القابلة لإعادة الاستخدام.

يمكن استخدامها في:

- التقارير.
- المستندات.
- الرسائل.
- الإشعارات.
- المطبوعات.

---

#### 5.3.7 البحث — Search

خدمة بحث موحدة عند وجود حاجة مشتركة لذلك.

يمكن أن توفر:

- البحث النصي.
- Filtering.
- Sorting.
- Pagination.
- البحث عبر أكثر من نوع من الكيانات عند الحاجة.

لا يعني ذلك أن كل عمليات البحث يجب أن تمر عبر محرك بحث مركزي؛ بل يتم استخدام الخدمة عندما يكون ذلك مناسبًا.

---

#### 5.3.8 النشاط — Activity

خدمة لعرض النشاطات المتعلقة بالكيانات أو المستخدمين أو التطبيقات.

مثال:

```text id="j0z4g7"
Invoice #1024

Created by Ahmed
Edited by Mohamed
Approved by Ali
Attachment added
Status changed
```

يجب التمييز بين:

**Audit** → سجل أمني/تشغيلي موثوق للعمليات.

**Activity** → عرض زمني مناسب للمستخدم للنشاطات.

---

#### 5.3.9 الاستيراد والتصدير — Import / Export

خدمات مشتركة لمعالجة البيانات القادمة من أو الخارجة إلى أنظمة أو ملفات أخرى.

أمثلة:

- CSV
- Excel
- JSON
- APIs

ويجب أن توفر المنصة آليات مشتركة عند تكرار نفس احتياجات الاستيراد والتصدير بين التطبيقات.

---

#### 5.3.10 المهام الخلفية — Background Jobs

توفر المنصة آلية موحدة لتنفيذ العمليات غير المتزامنة أو طويلة المدة.

أمثلة:

- معالجة ملفات كبيرة.
- إنشاء تقارير.
- إرسال عدد كبير من التنبيهات.
- عمليات الاستيراد.
- مهام دورية.

---

#### 5.3.11 الأحداث — Events / Event Bus

توفر المنصة آلية موحدة لنشر واستهلاك الأحداث.

```text id="mlz5ah"
Application
     │
     │ Event
     ▼
 Event Bus
     │
     ├── Application
     ├── Notification
     ├── Audit
     └── Integration
```

يتم استخدام الأحداث عندما تحقق فائدة حقيقية في تقليل الترابط بين المكونات.

---

#### 5.3.12 Webhooks

خدمة لإرسال أحداث أو إشعارات إلى أنظمة خارجية عند حدوث أحداث محددة.

مثال:

```text id="pjjqpf"
OrderConfirmed
      ↓
Webhook
      ↓
External System
```

---

#### 5.3.13 التكاملات — Integrations

توفر المنصة طبقة موحدة للتعامل مع الأنظمة والخدمات الخارجية.

أمثلة:

- External APIs
- Payment Providers
- Email Providers
- SMS Providers
- Accounting Systems
- Other External Platforms

يجب عزل تفاصيل مزود الخدمة الخارجي عن منطق الأعمال كلما كان ذلك ممكنًا.

---

### 5.4 قاعدة إضافة المكونات إلى الـ Core

لا تتم إضافة مكون جديد إلى **Core Kernel** لمجرد أنه مستخدم من أكثر من Application.

يجب أن تتوفر فيه طبيعة مركزية للمنصة، مثل ارتباطه بـ:

- هوية المستخدم.
- الأمان.
- إدارة المؤسسات.
- تشغيل المنصة.
- إدارة التطبيقات.
- أو قواعد أساسية تعتمد عليها معظم أجزاء النظام.

أما الخدمات المشتركة التي يمكن أن تكون أكثر استقلالًا، فيُفضل إبقاؤها ضمن **Shared Services**.

---

### 5.5 مبدأ صغر النواة — Keep the Core Small

> **كلما أمكن إبقاء الوظيفة خارج الـ Core دون الإضرار بالمعمارية، يفضل إبقاؤها خارج الـ Core.**

الهدف هو الحفاظ على نواة:

- صغيرة.
- مستقرة.
- موثوقة.
- قليلة التغيير.
- يعتمد عليها الجميع.

بينما يمكن أن تتطور الخدمات المشتركة والتطبيقات بوتيرة أكبر.

---

### 5.6 التطور المستقبلي

لا يُشترط أن تكون جميع هذه المكونات عمليات مستقلة أو Microservices.

يمكن أن تبدأ داخل بنية تشغيل موحدة مع الحفاظ على الحدود المنطقية والعقود الواضحة.

يمكن فصل أي خدمة أو Module مستقبلًا عندما يصبح ذلك مبررًا بسبب:

- الحجم.
- الأداء.
- الاستقلال في دورة الإصدار.
- متطلبات التوسع.
- متطلبات الأمان.
- أو احتياجات تشغيلية أخرى.

ولا يتم الفصل لمجرد الرغبة في زيادة عدد الخدمات.

# 6. حدود التطبيقات (Application Boundaries)

يمثل كل Application نطاقًا وظيفيًا مستقلًا يملك منطق الأعمال الخاص به، ويعتمد على خدمات المنصة من خلال عقود واضحة.

## 6.1 استقلال التطبيق

يجب أن يكون كل Application:

- مستقلًا وظيفيًا.
- مسؤولًا عن منطق الأعمال الخاص بمجاله.
- مسؤولًا عن بيانات المجال التي يملكها.
- معتمدًا على خدمات المنصة من خلال عقود واضحة.
- قادرًا على التطور دون الاعتماد غير الضروري على تطبيقات أخرى.

## 6.2 ما لا يملكه التطبيق

لا يكون التطبيق مسؤولًا عن بناء أو إدارة:

- نظام الهوية المركزي.
- نظام المصادقة المركزي.
- نظام الصلاحيات المركزي.
- نظام الإعدادات المركزي.
- نظام التنبيهات المركزي.
- نظام الملفات المركزي.
- نظام التدقيق المركزي.
- أي خدمة مشتركة توفرها المنصة بالفعل.

## 6.3 استقلال التطبيقات عن بعضها

يجب تجنب الاعتماد المباشر غير الضروري بين Applications.

لا ينبغي لتطبيق أن يعتمد على:

- تفاصيل التنفيذ الداخلية لتطبيق آخر.
- جداول قاعدة بيانات يملكها تطبيق آخر.
- Classes أو Functions داخلية لتطبيق آخر.

عند الحاجة إلى التواصل بين تطبيقين، يجب استخدام Contract أو API أو Event مناسب.

---

# 7. معمارية الموديولات (Module Architecture)

يمثل الـ Module وحدة وظيفية واضحة داخل Application أو داخل المنصة.

يجب أن يمتلك كل Module حدودًا واضحة ومسؤوليات محددة.

## 7.1 مكونات الـ Module

يمكن أن يتضمن الـ Module، حسب طبيعته:

- Domain / Business Logic
- Data
- API
- UI
- Permissions
- Settings
- Events
- Reports

ليس من الضروري أن يحتوي كل Module على جميع هذه المكونات.

## 7.2 ملكية الـ Module

يجب أن يكون واضحًا:

- من يملك الـ Module.
- ما المجال الذي يغطيه.
- ما البيانات التي يملكها.
- ما الخدمات التي يستخدمها.
- ما الخدمات التي يقدمها.
- ما الأحداث التي ينشرها.
- ما الأحداث التي يستجيب لها.

## 7.3 تسجيل الـ Module

يتم تسجيل الـ Module من خلال `Module Registry`.

يسمح ذلك للمنصة بمعرفة:

- اسم الـ Module.
- الإصدار.
- الحالة.
- المسارات.
- الصلاحيات.
- الإعدادات.
- الاعتماديات.
- المكونات التي يقدمها.

---

# 8. معمارية الاتصال (Communication Architecture)

يتم التواصل بين مكونات النظام من خلال آليات محددة تعتمد على طبيعة العلاقة المطلوبة.

## 8.1 الاتصال المتزامن (Synchronous)

يستخدم عندما تكون هناك حاجة إلى نتيجة مباشرة.

الآليات المحتملة:

- APIs
- Service Contracts
- Direct Service Calls داخل الحدود المناسبة

يجب ألا يستخدم الاتصال المباشر للوصول إلى تفاصيل تنفيذ مكون آخر.

## 8.2 الاتصال غير المتزامن (Asynchronous)

يستخدم عندما لا تكون الاستجابة الفورية ضرورية أو عندما يكون فصل المكونات مفيدًا.

الآليات:

- Events
- Queues
- Background Jobs
- Scheduled Tasks

## 8.3 اختيار آلية الاتصال

يجب اختيار آلية الاتصال بناءً على طبيعة العملية وليس بناءً على تفضيل تقني مجرد.

بشكل عام:

```text id="v8d5k2"
هل نحتاج نتيجة فورية؟
        │
   ┌────┴────┐
   │         │
  نعم       لا
   │         │
   ▼         ▼
Sync       Async
   │         │
 API /     Event /
Contract   Queue / Job
```

---

# 9. معمارية الأحداث (Event Architecture)

توفر المنصة آلية موحدة لنشر واستهلاك الأحداث بين المكونات.

```text id="c9l3sp"
Application
     │
     │ publishes Event
     ▼
  Event Bus
     │
     ├── Application
     ├── Shared Service
     ├── Notification
     └── Audit
```

## 9.1 استقلال الحدث

يجب أن يكون الـ Event مستقلًا قدر الإمكان عن المستهلكين الذين يستخدمونه.

يجب ألا يحتوي الحدث على تفاصيل تنفيذ داخلية لا يحتاج إليها المستهلك.

## 9.2 استخدام الأحداث

تستخدم الأحداث عندما تحقق فائدة حقيقية مثل:

- تقليل الترابط.
- فصل المسؤوليات.
- تشغيل عمليات غير متزامنة.
- إتاحة التكامل مع أكثر من مستهلك.

لا يجب تحويل كل عملية إلى Event لمجرد وجود Event Bus.

## 9.3 ملكية الأحداث

يجب أن يكون واضحًا:

- من ينشر الحدث.
- معنى الحدث.
- البيانات التي يحملها.
- المستهلكون المحتملون.
- توافق الإصدارات عند الحاجة.

---

# 10. معمارية الهوية والصلاحيات (Identity & Authorization Architecture)

تعتبر الهوية والمصادقة والصلاحيات من خدمات الـ Platform المركزية.

```text id="x3e9k1"
User
  │
  ▼
Organization
  │
  ▼
Role
  │
  ▼
Permissions
```

## 10.1 الهوية

تكون هوية المستخدم مركزية على مستوى المنصة، ولا يقوم كل Application بإنشاء هوية مستقلة للمستخدم.

## 10.2 المصادقة

تتم المصادقة من خلال آلية موحدة توفرها المنصة.

التطبيقات تعتمد على نتيجة المصادقة ولا تنفذ نظام Login مستقلًا.

## 10.3 الصلاحيات

توفر المنصة نظامًا موحدًا للصلاحيات.

مثال:

```text id="y2g4m8"
inventory.item.view
inventory.item.create
inventory.item.edit
inventory.item.delete
inventory.item.approve
```

يمكن توسيع النموذج مستقبلًا لدعم Scopes أو قيود إضافية عند الحاجة.

## 10.4 التطبيقات

يمكن لكل Application تعريف الصلاحيات الخاصة بمجاله، لكن تسجيلها وإدارتها يتم من خلال نظام الصلاحيات المركزي.

---

# 11. معمارية الإعدادات (Settings Architecture)

توفر المنصة آلية موحدة لإدارة الإعدادات.

تقسم الإعدادات منطقيًا إلى مستويات:

```text id="g1z7s4"
Platform Settings
       │
Organization Settings
       │
Application Settings
       │
User Settings
```

## 11.1 Platform Settings

الإعدادات العامة التي تؤثر على تشغيل المنصة.

## 11.2 Organization Settings

الإعدادات الخاصة بالمؤسسة أو نطاق العمل.

## 11.3 Application Settings

الإعدادات الخاصة بتطبيق معين.

## 11.4 User Settings

الإعدادات والتفضيلات الخاصة بالمستخدم.

يجب أن توفر المنصة آلية موحدة للوصول إلى الإعدادات وإدارتها، مع تحديد أولوية المستويات وقواعد الوراثة عند تنفيذ النظام.

---

# 12. التوطين واللغة (Localization)

تدعم المنصة التوطين من البداية بدل بناء نظام مستقل داخل كل Application.

تشمل مسؤوليات Localization:

- Multiple Languages
- Translation
- RTL / LTR
- Date Formats
- Number Formats
- Currency
- Timezone

يجب فصل النصوص القابلة للترجمة عن منطق الأعمال وواجهة المستخدم.

---

# 13. معمارية واجهة المستخدم (UI Architecture)

توفر المنصة طبقة UI مشتركة لضمان الاتساق وإعادة الاستخدام.

## 13.1 Design System

يشمل مبدئيًا:

- Buttons
- Inputs
- Selects
- Forms
- Tables
- Cards
- Filters
- Modals
- Navigation
- Notifications
- Loading States
- Empty States
- Error States

## 13.2 مسؤولية التطبيقات

يستخدم التطبيق المكونات المشتركة عند توفرها.

ويجوز له إنشاء مكونات خاصة بالمجال عندما تكون هناك حاجة حقيقية لذلك.

## 13.3 مبدأ عدم التقييد

لا تهدف طبقة UI المشتركة إلى منع التطبيقات من بناء واجهات خاصة بها.

الهدف هو:

> **توفير أساس مشترك، وليس فرض واجهة واحدة على كل تطبيق.**

---

# 14. الملفات والتخزين (Files & Storage)

تعتبر إدارة الملفات والمرفقات خدمة مشتركة للمنصة.

```text id="n4k6x2"
Entity
   │
   └── Attachments
          │
          └── File Storage
```

تتعامل الخدمة مع:

- رفع الملفات.
- تخزين الملفات.
- الوصول إليها.
- بيانات الملف.
- ربط الملفات بالكيانات.
- التحكم في الوصول.
- الإصدارات عند الحاجة.

يجب ألا يقوم كل Application ببناء نظام تخزين مستقل للملفات المشتركة.

يجب فصل مفهوم التخزين الفعلي للملف عن منطق استخدامه داخل التطبيق.

---

# 15. التقارير والطباعة (Reporting & Printing)

توفر المنصة خدمات مشتركة لإنتاج التقارير والمستندات والمخرجات القابلة للطباعة أو التصدير.

```text id="h6q3w9"
Application Data
      ↓
Report Definition
      ↓
Template
      ↓
Renderer
      ↓
PDF / Print / Export
```

## 15.1 فصل البيانات عن العرض

يجب ألا يرتبط منطق الأعمال مباشرة بتنسيق التقرير أو طريقة طباعته.

## 15.2 أنواع المخرجات

يمكن أن تدعم المنصة، حسب الحاجة:

- PDF
- Print
- Excel
- CSV
- Other Export Formats

## 15.3 التقارير الخاصة بالتطبيق

يمكن لكل Application تعريف التقارير الخاصة بمجاله، بينما توفر المنصة البنية والخدمات المشتركة اللازمة لتوليدها.

التفاصيل التقنية الخاصة بمحرك التقارير والقوالب والطباعة سيتم تحديدها لاحقًا بقرارات معمارية مستقلة.

## 16. معمارية التدقيق والنشاط (Audit & Activity Architecture)

توفّر المنصة آلية موحدة لتسجيل العمليات المهمة وعرض النشاطات المرتبطة بالكيانات والمستخدمين والتطبيقات.

يتم الفصل بوضوح بين مفهوم **Audit** ومفهوم **Activity**، رغم إمكانية ارتباطهما ببعضهما في بعض الحالات.

### 16.1 التدقيق — Audit

يمثل الـ Audit سجلًا موثوقًا للعمليات المهمة التي تحدث داخل النظام.

يستخدم لأغراض مثل:

- المراجعة.
- التحقيق في العمليات.
- تتبع التغييرات.
- المتطلبات الأمنية والتشغيلية.
- معرفة مصدر التغيير وتوقيته.

يجب أن يسمح سجل التدقيق، حسب طبيعة العملية، بمعرفة:

- من قام بالعملية.
- ماذا حدث.
- متى حدث.
- على أي كيان حدثت العملية.
- ما العملية التي تم تنفيذها.
- القيم السابقة عند الحاجة.
- القيم الجديدة عند الحاجة.
- مصدر العملية عند الحاجة.

ولا ينبغي أن يعتمد كل Application على إنشاء نظام Audit مستقل إذا كانت العملية يمكن تسجيلها من خلال خدمة المنصة.

### 16.2 النشاط — Activity

يمثل الـ Activity عرضًا موجهًا للمستخدم للنشاطات التي حدثت داخل النظام.

يمكن أن يظهر النشاط على مستوى:

- الكيان.
- المستخدم.
- التطبيق.
- المؤسسة.
- العملية.

مثال:

```text
Invoice #1024

Created by Ahmed
Edited by Mohamed
Approved by Ali
Attachment added
Status changed
```

الغرض من Activity هو توفير Timeline واضح ومفيد للمستخدم، وليس بالضرورة أن يكون سجلًا أمنيًا كاملًا.

### 16.3 الفرق بين Audit و Activity

يجب الحفاظ على الفصل المفاهيمي بينهما:

Audit
↓
Trusted Operational / Security Record

Activity
↓
User-Facing Activity Timeline

قد ينتج Activity من عملية تم تسجيلها في Audit، لكن لا يجب افتراض أن كل Activity يمثل Audit Record كاملًا، أو أن كل Audit Record يجب عرضه للمستخدم كنشاط.

### 16.4 مسؤولية المنصة والتطبيقات

توفر المنصة الآليات المشتركة اللازمة للتدقيق والنشاط.

ويكون على Application:

تحديد العمليات الخاصة بمجاله التي تحتاج إلى تسجيل.
توفير البيانات الخاصة بالعملية التي لا تستطيع المنصة معرفتها تلقائيًا.
استخدام عقود المنصة بدل إنشاء أنظمة مستقلة عند توفر الخدمة المشتركة.

وتظل مسؤولية المنصة هي توفير البنية المشتركة لتسجيل وعرض هذه المعلومات.

### 16.5 مبدأ قابلية التتبع

يجب أن تكون العمليات المهمة قابلة للتتبع بطريقة تسمح بفهم:

- Who
- What
- When
- Which Entity
- What Changed

بحسب طبيعة العملية ومستوى التتبع المطلوب.

### 16.6 الحالة الحالية

لا تحدد هذه الوثيقة في هذه المرحلة:

- تقنية تخزين سجلات Audit.
- شكل قاعدة البيانات النهائي.
- أداة أو نظام Activity Timeline.
- سياسة الاحتفاظ بالسجلات (Retention).
- آلية أرشفة السجلات.
- تفاصيل الأداء أو حجم السجلات.

يتم تحديد هذه التفاصيل لاحقًا عند الحاجة من خلال قرارات معمارية مستقلة.

## 17. معمارية الأمان (Security Architecture)

يُعتبر الأمان مسؤولية مشتركة عبر جميع طبقات النظام، مع توفير القدرات المركزية الأساسية من خلال الـ Platform.

يجب أن يكون الأمان جزءًا من التصميم الأساسي للنظام، وليس وظيفة تتم إضافتها لاحقًا لكل Application بشكل مستقل.

### 17.1 المصادقة — Authentication

تتم المصادقة من خلال آلية موحدة توفرها المنصة.

يجب ألا يقوم كل Application بإنشاء نظام Login أو إدارة جلسات مستقلة للمستخدمين.

وتكون مسؤولية المنصة عن:

- إثبات هوية المستخدم.
- إدارة جلسات الدخول.
- تسجيل الخروج.
- إدارة كلمات المرور.
- استعادة الوصول.
- آليات التحقق من الهوية حسب ما يتم اعتماده لاحقًا.

يمكن إضافة آليات مثل MFA أو SSO عند وجود حاجة فعلية إليها.

### 17.2 التفويض — Authorization

توفر المنصة نظامًا مركزيًا للتحكم في الوصول إلى الوظائف والموارد.

يمكن لكل Application تعريف الصلاحيات الخاصة بمجاله، بينما تتم إدارة آلية التحقق من الصلاحيات من خلال النظام المركزي.

يجب أن تعتمد التطبيقات على مبدأ:

```text
User
  ↓
Role
  ↓
Permissions
  ↓
Allowed Operation
```

ولا يجوز اعتبار وجود المستخدم المصادق عليه وحده كافيًا للوصول إلى وظيفة أو مورد.

### 17.3 أقل صلاحية لازمة — Least Privilege

يجب أن يحصل كل مستخدم أو Module أو Service على الحد الأدنى من الصلاحيات اللازمة لأداء مهمته.

ويجب تجنب منح صلاحيات أوسع من المطلوب لمجرد سهولة التنفيذ.

### 17.4 الأمان الافتراضي — Secure Defaults

يجب أن تكون الحالة الافتراضية للمكونات الجديدة آمنة قدر الإمكان.

ويجب ألا يتطلب الاستخدام الطبيعي للنظام تعطيل آليات الحماية أو إضافة إعدادات أمنية أساسية يدويًا.

### 17.5 حماية البيانات

يجب حماية البيانات من الوصول أو التعديل غير المصرح به.

وتشمل الحماية، حسب طبيعة البيانات والبيئة:

- التحكم في الوصول.
- التحقق من الصلاحيات.
- حماية البيانات الحساسة.
- حماية الاتصالات.
- إدارة الأسرار بشكل آمن.
- عدم وضع الأسرار داخل Source Code.

### 17.6 التحقق عند حدود النظام

يجب عدم الوثوق بالبيانات القادمة من المستخدم أو من الأنظمة الخارجية بشكل تلقائي.

يجب التحقق من البيانات عند حدود النظام، بما في ذلك:

- User Input.
- API Requests.
- Uploaded Files.
- External Integrations.
- Imported Data.

### 17.7 تسجيل العمليات الحساسة

يجب تسجيل العمليات الحساسة والمهمة من خلال نظام Audit المركزي عندما يكون ذلك مطلوبًا.

ويجب أن يكون التسجيل متسقًا عبر التطبيقات بدل إنشاء آليات مستقلة لكل Application.

### 17.8 مسؤولية التطبيقات

تلتزم التطبيقات بمتطلبات الأمان التي توفرها المنصة، كما تتحمل مسؤولية حماية قواعد الأعمال والبيانات الخاصة بمجالها.

ولا يجوز للتطبيق تجاوز آليات الأمان المركزية أو إنشاء آلية بديلة تؤدي إلى إضعاف النموذج الأمني العام.

### 17.9 الحالة الحالية

لا تحدد هذه الوثيقة في هذه المرحلة:

- تقنية المصادقة النهائية.
- مزود الهوية.
- آلية MFA.
- آلية SSO.
- تقنية تشفير محددة.
- نظام إدارة الأسرار.
- أدوات فحص الأمان.
- تفاصيل Security Monitoring.

يتم تحديد هذه التفاصيل لاحقًا من خلال قرارات معمارية مستقلة عند الحاجة.

## 18. معمارية واجهات البرمجة والعقود (API Architecture)

توفر المنصة والتطبيقات واجهات وعقودًا واضحة للتواصل بين المكونات والأنظمة عند الحاجة.

يجب أن تكون الـ APIs قابلة للفهم والاختبار والتطور دون كسر المستهلكين الحاليين دون تخطيط.

### 18.1 دور الـ API

يمكن استخدام الـ API عندما تحتاج مكونات النظام أو الأنظمة الخارجية إلى الوصول إلى وظيفة أو بيانات من خلال واجهة محددة.

ويجب أن يمثل الـ API عقدًا واضحًا بين مقدم الخدمة والمستهلك.

### 18.2 العقود — Contracts

يجب ألا يعتمد المستهلك على تفاصيل التنفيذ الداخلية للخدمة.

ويجب أن يحدد العقد، حسب طبيعة الواجهة:

- العمليات المتاحة.
- المدخلات.
- المخرجات.
- الأخطاء المتوقعة.
- متطلبات الوصول.
- قواعد التوافق.

يمكن تغيير التنفيذ الداخلي طالما ظل العقد المتفق عليه صالحًا.

### 18.3 أنواع الواجهات

يمكن أن تستخدم المعمارية أنواعًا مختلفة من الواجهات بحسب طبيعة العلاقة، مثل:

- Platform APIs.
- Application APIs.
- Internal Service Contracts.
- External APIs.
- Integration APIs.

ولا يشترط استخدام آلية واحدة لجميع أنواع التواصل.

### 18.4 API بين التطبيقات

عند حاجة Application إلى وظيفة أو بيانات يملكها Application آخر، يجب ألا يتم الوصول مباشرة إلى تفاصيل التنفيذ أو جداول البيانات الداخلية.

يمكن استخدام:

```text
Application A
      ↓
API / Contract
      ↓
Application B
```

بحسب طبيعة العلاقة.

### 18.5 الوصول والصلاحيات

يجب أن تخضع APIs لآليات المصادقة والتفويض المناسبة.

ولا يعني وجود API أن جميع المستخدمين أو التطبيقات يملكون صلاحية استخدامها.

### 18.6 التوافق والتطور

يجب تصميم APIs بحيث تسمح بالتطور التدريجي.

لا يجوز إجراء تغيير غير متوافق مع المستهلكين الحاليين دون وجود استراتيجية مناسبة، مثل:

- Backward Compatibility.
- Versioning.
- Migration.
- Deprecation.

### 18.7 خصائص API

يجب أن تهدف الواجهات إلى أن تكون:

- واضحة.
- متوقعة.
- قابلة للاختبار.
- قابلة للتوثيق.
- قابلة للتطور.
- متوافقة مع المستهلكين قدر الإمكان.

### 18.8 الحالة الحالية

لا تحدد هذه الوثيقة في هذه المرحلة:

- نمط API النهائي.
- REST أو GraphQL أو غيرهما.
- صيغة Versioning النهائية.
- صيغة الأخطاء.
- Pagination.
- Filtering.
- Sorting.
- Rate Limiting.
- آلية توثيق الـ API.
- التقنية المستخدمة لتنفيذ الـ API.

يتم تحديد هذه التفاصيل لاحقًا من خلال قرارات معمارية مستقلة.

## 19. معمارية البيانات (Data Architecture)

تعتمد المعمارية على مبدأ واضح لملكية البيانات، بحيث يكون لكل نطاق أو Application مسؤولية واضحة عن بيانات المجال التي يملكها.

### 19.1 ملكية البيانات — Data Ownership

كل Application مسؤول عن بيانات المجال الخاصة به.

مثال:

```text
Inventory
   └── Inventory Data

Sales
   └── Sales Data

Finance
   └── Financial Data
```

ولا ينبغي لتطبيق أن يتعامل مباشرة مع البيانات الداخلية التي يملكها تطبيق آخر.

### 19.2 الوصول إلى بيانات Application آخر

عندما يحتاج Application إلى بيانات مملوكة لـ Application آخر، يجب استخدام آلية اتصال معتمدة.

مثل:

- API.
- Service Contract.
- Event.
- Integration Contract.

بحسب طبيعة الحاجة.

### 19.3 عدم الوصول المباشر إلى الجداول

لا يجوز أن يعتمد Application على جداول أو تفاصيل تخزين داخلية مملوكة لتطبيق آخر.

مثال غير مرغوب:

```text
Sales
  │
  └──── Direct Database Access ────► Inventory Tables
```

والبديل:

```text
Sales
  │
  ▼
Contract / API / Event
  │
  ▼
Inventory
```

الهدف هو حماية استقلالية التطبيقات وإتاحة تغيير طريقة تخزين البيانات داخليًا دون كسر المستهلكين.

### 19.4 بيانات المنصة

تمتلك المنصة البيانات المرتبطة بالقدرات المركزية التي تديرها، مثل:

- Users.
- Organizations.
- Roles.
- Permissions.
- Settings.
- Modules.
- Audit.

بينما تمتلك التطبيقات بيانات المجالات الخاصة بها.

### 19.5 العلاقات بين البيانات

يمكن أن توجد علاقات منطقية بين بيانات تطبيقات مختلفة، لكن يجب ألا تؤدي هذه العلاقات إلى كسر حدود الملكية.

يجب أن يكون الوصول إلى البيانات الخارجية من خلال آلية واضحة ومناسبة لطبيعة العلاقة.

### 19.6 المعاملات — Transactions

يجب أن تكون المعاملات مرتبطة بحدود الملكية والمسؤولية المناسبة.

ولا ينبغي إنشاء معاملات مترابطة بين مجالات متعددة دون وجود حاجة معمارية واضحة.

تفاصيل استراتيجية المعاملات بين التطبيقات سيتم تحديدها لاحقًا عند الحاجة.

### 19.7 البيانات المشتركة

يجب تجنب إنشاء بيانات مشتركة بين التطبيقات دون تحديد مالك واضح لها.

إذا أصبحت بيانات معينة ذات طبيعة مشتركة فعلًا، فيجب تحديد:

- مالك البيانات.
- طريقة الوصول إليها.
- المسؤول عن تحديثها.
- العقد المستخدم للوصول إليها.
- قواعد التوافق.

### 19.8 الحالة الحالية

لا تحدد هذه الوثيقة في هذه المرحلة:

- نوع قاعدة البيانات.
- نموذج التخزين النهائي.
- Database Schema.
- استراتيجية مشاركة قواعد البيانات.
- استراتيجية Cache.
- Search Indexing.
- Backup.
- Retention.
- Data Archiving.
- تفاصيل المعاملات الموزعة.

يتم تحديد هذه التفاصيل لاحقًا بناءً على Tech Stack ونموذج التشغيل والاحتياجات الفعلية.

## 20. معمارية المعالجة في الخلفية (Background Processing Architecture)

توفر المنصة آلية موحدة لتنفيذ العمليات التي لا تحتاج إلى تنفيذها ضمن الطلب المباشر أو التي قد تستغرق وقتًا طويلًا.

يتم استخدام المعالجة في الخلفية عندما تحقق فائدة حقيقية، وليس لمجرد زيادة تعقيد النظام.

### 20.1 متى تستخدم المعالجة في الخلفية؟

يمكن استخدام Background Processing عندما تكون العملية:

- طويلة المدة.
- مكلفة من ناحية الموارد.
- غير ضرورية لإكمال الاستجابة الفورية.
- قابلة للتنفيذ بشكل غير متزامن.
- دورية أو مجدولة.
- تحتاج إلى إعادة المحاولة عند الفشل.

### 20.2 أمثلة

تشمل الحالات المحتملة:

- إنشاء التقارير.
- معالجة الملفات الكبيرة.
- عمليات الاستيراد.
- إرسال عدد كبير من التنبيهات.
- معالجة أحداث النظام.
- عمليات التكامل الخارجية.
- المهام الدورية.

### 20.3 آليات المعالجة

يمكن أن تعتمد المنصة على آليات مثل:

```text
Request
   │
   ▼
Queue
   │
   ▼
Job
   │
   ▼
Worker
   │
   ▼
Result
```

كما يمكن استخدام:

- Queues.
- Jobs.
- Workers.
- Scheduled Tasks.

بحسب طبيعة العملية.

### 20.4 الفصل عن الطلب المباشر

لا ينبغي تنفيذ عملية طويلة أو مكلفة داخل Request مباشر إذا كان ذلك يؤدي إلى:

- بطء الاستجابة.
- استهلاك موارد غير مناسب.
- احتمالية انتهاء المهلة.
- تأثير العملية على العمليات الأخرى.

في هذه الحالات يمكن نقل العملية إلى Background Job.

### 20.5 إعادة المحاولة والتعامل مع الفشل

يجب أن تراعي المهام الخلفية احتمالية الفشل.

وبحسب طبيعة العملية، يمكن أن تتضمن الآلية:

- Retry.
- Failure Handling.
- Logging.
- Monitoring.
- منع التكرار غير المقصود عند الحاجة.

ويجب أن تكون العمليات التي يمكن إعادة تنفيذها مصممة بحيث لا تؤدي إعادة المحاولة إلى نتائج غير متوقعة متى كان ذلك ممكنًا.

### 20.6 المهام المجدولة — Scheduled Tasks

يمكن استخدام المهام المجدولة لتنفيذ عمليات دورية، مثل:

- المعالجة الدورية.
- تنظيف البيانات المؤقتة.
- إرسال التذكيرات.
- إنشاء التقارير الدورية.
- مزامنة البيانات مع الأنظمة الخارجية.

ويجب أن تكون كل مهمة مجدولة مرتبطة بمسؤولية واضحة.

### 20.7 مسؤولية المنصة والتطبيقات

توفر المنصة آلية التشغيل المشتركة للمهام الخلفية.

بينما يحدد Application:

- العملية التي تحتاج إلى معالجة خلفية.
- البيانات المطلوبة.
- قواعد العمل الخاصة بالعملية.
- ما الذي يجب أن يحدث عند النجاح أو الفشل.

ولا يجب وضع قواعد الأعمال الخاصة بالتطبيق داخل البنية التحتية الخاصة بتشغيل الـWorkers.

### 20.8 الحالة الحالية

لا تحدد هذه الوثيقة في هذه المرحلة:

- تقنية Queue.
- Message Broker.
- عدد Workers.
- استراتيجية توزيع المهام.
- آلية Scaling.
- تقنية Scheduler.
- سياسات Retry التفصيلية.
- آلية مراقبة الـJobs.

يتم تحديد هذه التفاصيل لاحقًا من خلال قرارات معمارية مستقلة عند الحاجة.

```

**كده وصلنا إلى 20.**

والنقطة المهمة هنا: أنا حافظت على نفس المبدأ الذي اتفقنا عليه في الأقسام السابقة: **المعمارية تحدد الحدود والقواعد، أما اختيار Laravel Queue/Redis أو نوع الـAPI أو قاعدة البيانات أو أدوات الـMonitoring فنتركه للـADR.**

بعد اعتمادك لهذه الأربعة، ننتقل إلى **21 → 24**، ثم نختم بـ25 و26.
```

## 21. معمارية الرصد والمراقبة (Observability Architecture)

تهدف معمارية الرصد والمراقبة إلى توفير القدرة على معرفة حالة النظام وفهم الأخطاء والمشكلات التشغيلية وتأثيرها.

يجب أن تسمح آليات الرصد بالإجابة عن أسئلة أساسية مثل:

- هل النظام يعمل بشكل صحيح؟
- أين حدثت المشكلة؟
- متى حدثت؟
- ما المكون المتأثر؟
- ما تأثير المشكلة؟
- هل المشكلة متكررة؟

### 21.1 السجلات — Logging

يجب أن يوفر النظام آلية موحدة لتسجيل الأحداث والمعلومات التشغيلية المهمة.

يمكن أن تتضمن السجلات:

- العمليات التشغيلية.
- الأخطاء والاستثناءات.
- الأحداث المهمة.
- معلومات تشخيصية عند الحاجة.
- معلومات مرتبطة بالطلب أو العملية لتسهيل تتبعها.

يجب تجنب تسجيل البيانات الحساسة أو الأسرار داخل السجلات.

### 21.2 تتبع الأخطاء — Error Tracking

يجب أن تكون الأخطاء والاستثناءات قابلة للاكتشاف والتتبع.

ويجب أن تساعد آلية تتبع الأخطاء على معرفة:

- نوع الخطأ.
- مكان حدوثه.
- المكون المتأثر.
- السياق المرتبط بالخطأ عند الحاجة.
- مدى تكرار الخطأ.
- تأثيره على النظام.

### 21.3 المقاييس — Metrics

يمكن استخدام Metrics لقياس مؤشرات تشغيلية مهمة، مثل:

- عدد الطلبات.
- أزمنة الاستجابة.
- معدلات الأخطاء.
- حالة المهام الخلفية.
- استهلاك الموارد عند الحاجة.

ويتم تحديد المقاييس المطلوبة بناءً على احتياجات النظام الفعلية.

### 21.4 فحوصات الصحة — Health Checks

يجب أن توفر المنصة آليات تساعد على معرفة ما إذا كانت المكونات الأساسية للنظام قادرة على العمل.

يمكن أن تشمل Health Checks، حسب الحاجة:

- Application Availability.
- Database Connectivity.
- Queue Availability.
- Storage Availability.
- External Service Availability.

### 21.5 المراقبة والتنبيه — Monitoring & Alerting

يمكن استخدام المراقبة والتنبيهات لاكتشاف الحالات التي تحتاج إلى تدخل تشغيلي.

ويجب أن تكون التنبيهات مرتبطة بمشكلات ذات أهمية فعلية، مع تجنب إنشاء عدد كبير من التنبيهات غير المفيدة.

### 21.6 الحالة الحالية

لا تحدد هذه الوثيقة في هذه المرحلة:

- أداة Logging.
- نظام Error Tracking.
- منصة Metrics.
- نظام Monitoring.
- نظام Alerting.
- أدوات Distributed Tracing.
- قواعد التنبيه التفصيلية.

يتم تحديد هذه التفاصيل لاحقًا من خلال قرارات معمارية مستقلة عند الحاجة.

## 22. معمارية النشر (Deployment Architecture)

تهدف معمارية النشر إلى توفير طريقة قابلة للتكرار والإدارة لتشغيل المنصة والتطبيقات.

يجب أن تسمح المعمارية بتطوير النظام ونشره دون فرض تعقيد تشغيلي غير ضروري في المراحل الأولى.

### 22.1 البيئات — Environments

يمكن أن يعمل النظام في بيئات مختلفة بحسب احتياجات التطوير والتشغيل، مثل:

- Development.
- Testing.
- Staging.
- Production.

ويجب الفصل بين إعدادات وبيانات البيئات المختلفة.

### 22.2 قابلية تكرار النشر

يجب أن تكون عملية النشر قابلة للتكرار قدر الإمكان.

ويجب تجنب الاعتماد على خطوات يدوية غير موثقة تؤدي إلى اختلاف حالة النظام بين البيئات.

### 22.3 إعدادات البيئة والأسرار

يجب فصل إعدادات البيئة والأسرار عن Source Code.

ويشمل ذلك، حسب الحاجة:

- Database Credentials.
- API Keys.
- Secrets.
- Environment-specific Configuration.

ولا يجوز وضع الأسرار الحساسة داخل مستودع الكود.

### 22.4 قاعدة البيانات والتخزين

يجب أن تراعي عملية النشر التغييرات المطلوبة في:

- Database.
- File Storage.
- Queues.
- Cached Data.

بحيث لا يؤدي النشر إلى فقدان البيانات أو كسر المكونات القائمة.

### 22.5 النسخ الاحتياطي والاستعادة

يجب أن تكون البيانات المهمة قابلة للنسخ الاحتياطي والاستعادة وفق متطلبات النظام.

وتفاصيل Backup وRecovery يتم تحديدها بناءً على أهمية البيانات ومتطلبات التشغيل.

### 22.6 قابلية التوسع

يجب ألا تمنع المعمارية إمكانية توسيع النظام مستقبلًا عند الحاجة.

لكن لا يشترط أن يتم بناء بنية Distributed أو Scalable بشكل معقد منذ البداية إذا لم تكن هناك حاجة فعلية لذلك.

### 22.7 مبدأ عدم الإفراط في التعقيد التشغيلي

يجب أن يكون نموذج النشر مناسبًا لحجم النظام واحتياجاته الحالية.

لا يتم إدخال:

- Microservices Deployment.
- Containers.
- Orchestration.
- Distributed Infrastructure.

إلا عندما تكون هناك حاجة فعلية أو قرار معماري يبرر ذلك.

### 22.8 الحالة الحالية

لا تحدد هذه الوثيقة في هذه المرحلة:

- Hosting Provider.
- Container Strategy.
- Orchestration Platform.
- CI/CD Tool.
- Deployment Pipeline.
- Database Deployment Strategy.
- Backup Technology.
- Disaster Recovery Implementation.
- Scaling Strategy.

يتم تحديد هذه التفاصيل لاحقًا من خلال قرارات معمارية مستقلة عند الحاجة.

## 23. معمارية التكامل (Integration Architecture)

توفر المنصة طبقة واضحة للتعامل مع الأنظمة والخدمات الخارجية، بهدف عزل تفاصيل التكامل عن منطق الأعمال قدر الإمكان.

### 23.1 أنواع التكامل

يمكن أن تشمل التكاملات:

- External APIs.
- Webhooks.
- Third-party Services.
- Import / Export.
- Integration Events.
- External Platforms.

ويتم اختيار آلية التكامل بحسب طبيعة النظام الخارجي والعملية المطلوبة.

### 23.2 عزل مزود الخدمة

يجب تجنب ربط Business Logic مباشرة بتفاصيل مزود خارجي متى كان ذلك ممكنًا.

النموذج المفضل:

```text
Application
     │
     ▼
Integration Contract
     │
     ▼
Provider Adapter
     │
     ▼
External Service
```

````

يساعد هذا الفصل على تغيير أو استبدال مزود الخدمة دون الحاجة إلى إعادة بناء منطق الأعمال بالكامل.

### 23.3 مسؤولية Application

يحدد Application:

- الحاجة إلى التكامل.
- قواعد العمل المرتبطة بالتكامل.
- البيانات المطلوبة.
- الأحداث أو العمليات التي تؤدي إلى التكامل.

ولا ينبغي أن يحتوي منطق الأعمال على تفاصيل تقنية خاصة بمزود خارجي إلا عند الضرورة.

### 23.4 مسؤولية Integration Layer

تتولى طبقة التكامل، حسب طبيعة النظام:

- الاتصال بالخدمة الخارجية.
- تحويل البيانات بين الأنظمة.
- التعامل مع تنسيق الطلبات والاستجابات.
- التعامل مع أخطاء الاتصال.
- إدارة تفاصيل Provider-specific.

### 23.5 Webhooks

يمكن استخدام Webhooks لاستقبال أو إرسال إشعارات مرتبطة بأحداث معينة.

ويجب التحقق من البيانات القادمة من الأنظمة الخارجية قبل استخدامها داخل النظام.

### 23.6 Import / Export

يمكن استخدام آليات Import / Export لتبادل البيانات مع الأنظمة أو الملفات الخارجية.

ويجب أن تتم معالجة البيانات المستوردة والتحقق منها قبل إدخالها إلى بيانات المجال.

### 23.7 فشل التكامل

يجب أن تراعي التكاملات احتمالية:

- انقطاع الخدمة الخارجية.
- Timeout.
- Invalid Response.
- Authentication Failure.
- Temporary Failure.

وبحسب طبيعة التكامل، يمكن استخدام:

- Retry.
- Background Jobs.
- Logging.
- Error Handling.
- Manual Recovery.

### 23.8 الحالة الحالية

لا تحدد هذه الوثيقة في هذه المرحلة:

- مزودي الخدمات الخارجيين.
- تقنية Integration Gateway.
- أدوات إدارة التكامل.
- شكل Provider Adapter النهائي.
- آلية Authentication مع كل مزود.
- تفاصيل Retry لكل تكامل.

يتم تحديد هذه التفاصيل لاحقًا عند الحاجة من خلال قرارات معمارية مستقلة.

## 24. الإصدارات والتوافق (Versioning & Compatibility)

يجب أن تسمح المعمارية بتطور المنصة والتطبيقات والموديولات والعقود دون كسر المكونات القائمة بصورة غير مخططة.

### 24.1 نطاق الإصدارات

يمكن أن تخضع للإصدار مكونات مختلفة، مثل:

- Platform.
- Applications.
- Modules.
- APIs.
- Contracts.
- Events.

ويتم تحديد أسلوب الإصدار المناسب لكل نوع عند الحاجة.

### 24.2 التوافق قبل التغيير

يجب تفضيل التغييرات المتوافقة مع المستهلكين الحاليين كلما كان ذلك ممكنًا.

ولا ينبغي إجراء تغيير في Contract أو API أو Event يؤدي إلى كسر المستهلكين الحاليين دون استراتيجية واضحة.

### 24.3 التغييرات غير المتوافقة — Breaking Changes

عندما يكون التغيير غير متوافق، يجب التخطيط للانتقال من النسخة القديمة إلى الجديدة.

يمكن أن يشمل ذلك:

- Versioning.
- Backward Compatibility.
- Migration.
- Deprecation.
- Transition Period.

### 24.4 إهمال الواجهات — Deprecation

عند الحاجة إلى إيقاف Contract أو API أو مكون، يجب إعطاء المستهلكين طريقة واضحة للانتقال إلى البديل عندما يكون ذلك ممكنًا.

ولا ينبغي إزالة واجهة مستخدمة دون معرفة تأثير ذلك على المستهلكين.

### 24.5 توافق الأحداث

يجب مراعاة التوافق عند تغيير Event Contract.

يجب ألا يؤدي تغيير بنية Event إلى كسر المستهلكين الحاليين دون وجود خطة مناسبة للتعامل مع التغيير.

### 24.6 مسؤولية التوافق

يجب على مالك الـContract أو الـAPI أو الـModule معرفة المستهلكين المتأثرين بالتغييرات المهمة.

ويجب توثيق التغييرات التي تؤثر على الحدود المعمارية أو العقود المشتركة.

### 24.7 الحالة الحالية

لا تحدد هذه الوثيقة في هذه المرحلة:

- Versioning Scheme النهائي.
- Semantic Versioning كقاعدة إلزامية.
- API Versioning Strategy.
- Event Versioning Strategy.
- مدة Deprecation.
- سياسة دعم الإصدارات القديمة.

يتم تحديد هذه التفاصيل لاحقًا من خلال قرارات معمارية مستقلة عند الحاجة.

## 25. الحالة الحالية للمعمارية (Current Architecture Status)

تمثل هذه الوثيقة في إصدارها الحالي التصور المعماري المعتمد حتى النقطة التي تمت مراجعتها واعتمادها.

### 25.1 الإصدار الحالي

الإصدار المعماري الحالي:

```text
v0.1
```

### 25.2 حالة المعمارية

الحالة الحالية:

```text
Architecture in Definition
```

وذلك يعني أن الهيكل والمبادئ الأساسية يتم تثبيتها تدريجيًا، بينما تظل بعض القرارات التقنية والتنفيذية قيد الدراسة.

### 25.3 ما تم تثبيته

تم تثبيت المفاهيم الأساسية المتعلقة بـ:

- الهدف العام للمنصة.
- أهداف المعمارية.
- المبادئ المعمارية.
- العلاقة بين Applications وPlatform وInfrastructure.
- طبقات المنصة.
- حدود Applications.
- معمارية Modules.
- آليات الاتصال.
- معمارية Events.
- Identity & Authorization.
- Settings.
- Localization.
- UI Architecture.
- Files & Storage.
- Reporting & Printing.
- Audit & Activity.
- Security.
- API Architecture.
- Data Architecture.
- Background Processing.
- Observability.
- Deployment.
- Integration.
- Versioning & Compatibility.

### 25.4 القرارات التقنية التي لم تُحسم بعد

لا تعتبر هذه الوثيقة بحد ذاتها قرارًا نهائيًا بشأن التقنيات المستخدمة في التنفيذ.

ومن أمثلة القرارات التي يمكن أن تحتاج إلى ADR مستقل:

- Tech Stack.
- Database.
- Frontend Architecture.
- Authentication Implementation.
- Authorization Implementation.
- Multi-Tenancy Model.
- API Strategy.
- Event Infrastructure.
- Storage Technology.
- Reporting Engine.
- Background Processing Infrastructure.
- Observability Stack.
- Deployment Model.
- CI/CD.
- Hosting.

### 25.5 قاعدة تحديث الحالة

يجب تحديث هذا القسم كلما تم اتخاذ قرار معماري جوهري أو تغيرت حالة أحد القرارات المهمة.

ولا ينبغي اعتبار أي تقنية أو آلية تنفيذية معتمدة لمجرد ذكرها في أمثلة أو مقترحات داخل هذه الوثيقة.

## 26. مراجع القرارات المعمارية (Architecture Decision References)

تحدد هذه الوثيقة الشكل العام للمعمارية والقواعد التي تحكمها، بينما يتم توثيق أسباب القرارات المعمارية والتقنية المهمة في ملف:

`ARCHITECTURE_DECISIONS.md`

### 26.1 الغرض من Architecture Decisions

يتم استخدام Architecture Decision Records (ADRs) لتوثيق القرارات التي يكون لها تأثير مهم على:

- بنية النظام.
- حدود المكونات.
- اختيار التقنيات.
- طريقة التواصل.
- إدارة البيانات.
- الأمان.
- التشغيل والنشر.
- قابلية التطور.

### 26.2 محتوى القرار

يجب أن يتضمن القرار، حسب الحاجة:

- السياق.
- المشكلة.
- المتطلبات.
- البدائل التي تمت دراستها.
- القرار.
- أسباب الاختيار.
- المزايا والآثار.
- العيوب أو التنازلات.
- التأثير على المعمارية.
- القرارات المرتبطة.

### 26.3 القرارات المقترحة

يمكن أن تتضمن القرارات المستقبلية، على سبيل المثال:

```text
ADR-001  Tech Stack
ADR-002  Database
ADR-003  Authentication
ADR-004  Authorization
ADR-005  Multi-Tenancy
ADR-006  Frontend Architecture
ADR-007  API Strategy
ADR-008  Event Infrastructure
ADR-009  Storage
ADR-010  Deployment
```

هذه القائمة تمثل أمثلة على قرارات محتملة وليست قرارات معتمدة مسبقًا.

### 26.4 العلاقة بين Architecture و ADR

العلاقة بين الوثائق تكون:

```text
Architecture Decision
        ↓
Architecture
        ↓
Platform Contract
        ↓
Implementation
```

يجب ألا يتم تغيير قرار معماري مهم بشكل غير معلن داخل الكود.

### 26.5 تغيير القرارات

عند تغير قرار معماري سابق، يجب الحفاظ على تاريخ القرار.

ولا يتم حذف القرار القديم لمجرد أنه لم يعد هو القرار الحالي.

يمكن إنشاء ADR جديد يوضح:

- القرار الجديد.
- سبب التغيير.
- القرار السابق الذي تم استبداله.
- تأثير التغيير.
- خطوات الانتقال عند الحاجة.

وبذلك تظل القرارات المعمارية قابلة للتتبع عبر تاريخ المشروع.

### 26.6 الحالة الحالية

لا تمثل أسماء ADRs المذكورة في هذا القسم قرارات منفذة أو معتمدة ما لم يوجد لها سجل فعلي في:

`ARCHITECTURE_DECISIONS.md`

ويجب إضافة مراجع ADR الفعلية إلى هذا القسم عند إنشاء القرارات واعتمادها.

```

كده يا برنس **أكملنا من 21 إلى 26**، وبذلك أصبح لدينا الهيكل الكامل للـ`ARCHITECTURE.md` من **1 إلى 26**.

وفي رأيي فيه **خطوة أخيرة مهمة جدًا قبل ما نقول الملف خلص**: بعد ما تضيف هذه الأقسام، نعمل مراجعة شاملة للملف كله مرة واحدة، خصوصًا لاكتشاف **التكرار والتعارض بين الأقسام**، ثم نُحدّث الـ`Table of Contents` ليعكس الـ26 قسمًا بالكامل.
```
````
